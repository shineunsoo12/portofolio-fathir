<!DOCTYPE html>
<html lang="id">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Portofolio - Mochammad Fathir Reza Fahresi</title>
<link href="https://fonts.googleapis.com/css2?family=Space+Mono:wght@400;700&family=Syne:wght@400;600;700;800&family=DM+Sans:wght@300;400;500&display=swap" rel="stylesheet">
<style>
  :root {
    --bg: #0a0a0f;
    --surface: #111118;
    --surface2: #1a1a24;
    --border: #2a2a3a;
    --accent: #00e5ff;
    --accent2: #7c3aed;
    --accent3: #f59e0b;
    --text: #e8e8f0;
    --text-muted: #7a7a8c;
    --glow: 0 0 30px rgba(0,229,255,0.15);
  }

  * { margin: 0; padding: 0; box-sizing: border-box; }

  body {
    background: var(--bg);
    color: var(--text);
    font-family: 'DM Sans', sans-serif;
    min-height: 100vh;
    overflow-x: hidden;
  }

  /* Background grid */
  body::before {
    content: '';
    position: fixed;
    inset: 0;
    background-image:
      linear-gradient(rgba(0,229,255,0.03) 1px, transparent 1px),
      linear-gradient(90deg, rgba(0,229,255,0.03) 1px, transparent 1px);
    background-size: 40px 40px;
    pointer-events: none;
    z-index: 0;
  }

  /* ── NAVBAR ── */
  nav {
    position: fixed;
    top: 0; left: 0; right: 0;
    z-index: 100;
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 18px 40px;
    background: rgba(10,10,15,0.85);
    backdrop-filter: blur(20px);
    border-bottom: 1px solid var(--border);
  }

  .nav-logo {
    font-family: 'Space Mono', monospace;
    font-size: 13px;
    color: var(--accent);
    letter-spacing: 2px;
    text-transform: uppercase;
  }

  .nav-tabs {
    display: flex;
    gap: 6px;
  }

  .nav-tab {
    font-family: 'Syne', sans-serif;
    font-size: 13px;
    font-weight: 600;
    letter-spacing: 1.5px;
    text-transform: uppercase;
    padding: 8px 20px;
    border-radius: 4px;
    border: 1px solid transparent;
    background: transparent;
    color: var(--text-muted);
    cursor: pointer;
    transition: all 0.25s ease;
  }

  .nav-tab:hover {
    color: var(--text);
    border-color: var(--border);
  }

  .nav-tab.active {
    background: rgba(0,229,255,0.08);
    border-color: var(--accent);
    color: var(--accent);
    box-shadow: 0 0 16px rgba(0,229,255,0.12);
  }

  /* ── SECTIONS ── */
  .section {
    display: none;
    min-height: 100vh;
    padding: 120px 40px 60px;
    position: relative;
    z-index: 1;
    animation: fadeUp 0.5s ease forwards;
  }

  .section.active { display: block; }

  @keyframes fadeUp {
    from { opacity: 0; transform: translateY(24px); }
    to   { opacity: 1; transform: translateY(0); }
  }

  .container { max-width: 960px; margin: 0 auto; }

  /* ── HOME ── */
  .home-grid {
    display: grid;
    grid-template-columns: 280px 1fr;
    gap: 60px;
    align-items: start;
  }

  .profile-card {
    position: sticky;
    top: 100px;
    background: var(--surface);
    border: 1px solid var(--border);
    border-radius: 16px;
    padding: 32px 24px;
    text-align: center;
    box-shadow: var(--glow);
  }

  .profile-photo-wrap {
    position: relative;
    width: 140px;
    height: 140px;
    margin: 0 auto 20px;
  }

  .profile-photo-wrap::before {
    content: '';
    position: absolute;
    inset: -3px;
    border-radius: 50%;
    background: conic-gradient(var(--accent), var(--accent2), var(--accent), var(--accent2), var(--accent));
    animation: spin 4s linear infinite;
    z-index: 0;
  }

  @keyframes spin { to { transform: rotate(360deg); } }

  .profile-photo {
    position: relative;
    z-index: 1;
    width: 140px;
    height: 140px;
    border-radius: 50%;
    object-fit: cover;
    border: 3px solid var(--bg);
    display: block;
  }

  .profile-name {
    font-family: 'Syne', sans-serif;
    font-size: 15px;
    font-weight: 800;
    color: var(--text);
    margin-bottom: 6px;
    line-height: 1.3;
  }

  .profile-badge {
    display: inline-block;
    font-family: 'Space Mono', monospace;
    font-size: 10px;
    color: var(--accent);
    background: rgba(0,229,255,0.08);
    border: 1px solid rgba(0,229,255,0.3);
    border-radius: 20px;
    padding: 4px 12px;
    margin-bottom: 6px;
    letter-spacing: 1px;
  }

  .profile-school {
    font-size: 12px;
    color: var(--text-muted);
    line-height: 1.5;
    margin-bottom: 20px;
  }

  .contact-links {
    display: flex;
    flex-direction: column;
    gap: 8px;
  }

  .contact-link {
    display: flex;
    align-items: center;
    gap: 10px;
    padding: 10px 14px;
    border-radius: 8px;
    background: var(--surface2);
    border: 1px solid var(--border);
    font-size: 12px;
    color: var(--text-muted);
    text-decoration: none;
    transition: all 0.2s;
    word-break: break-all;
  }

  .contact-link:hover { border-color: var(--accent); color: var(--accent); }

  .contact-icon { font-size: 14px; flex-shrink: 0; }

  /* About section */
  .about-content { padding-top: 8px; }

  .section-label {
    font-family: 'Space Mono', monospace;
    font-size: 11px;
    color: var(--accent);
    letter-spacing: 3px;
    text-transform: uppercase;
    margin-bottom: 12px;
  }

  .section-title {
    font-family: 'Syne', sans-serif;
    font-size: 36px;
    font-weight: 800;
    line-height: 1.2;
    margin-bottom: 8px;
  }

  .section-title span { color: var(--accent); }

  .divider {
    width: 48px;
    height: 3px;
    background: linear-gradient(90deg, var(--accent), var(--accent2));
    border-radius: 2px;
    margin-bottom: 28px;
  }

  .about-text {
    font-size: 15px;
    color: var(--text-muted);
    line-height: 1.85;
    margin-bottom: 18px;
  }

  .about-text strong { color: var(--text); font-weight: 500; }

  .info-cards {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 12px;
    margin-top: 28px;
  }

  .info-card {
    background: var(--surface);
    border: 1px solid var(--border);
    border-radius: 10px;
    padding: 16px;
    transition: border-color 0.2s;
  }

  .info-card:hover { border-color: var(--accent); }

  .info-card-label {
    font-family: 'Space Mono', monospace;
    font-size: 10px;
    color: var(--accent);
    letter-spacing: 1.5px;
    text-transform: uppercase;
    margin-bottom: 4px;
  }

  .info-card-value {
    font-size: 13px;
    font-weight: 500;
    color: var(--text);
  }

  /* ── SKILL ── */
  .page-header {
    margin-bottom: 48px;
  }

  .skill-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
    gap: 28px;
  }

  .skill-card {
    background: var(--surface);
    border: 1px solid var(--border);
    border-radius: 16px;
    overflow: hidden;
    transition: all 0.3s ease;
    position: relative;
  }

  .skill-card::before {
    content: '';
    position: absolute;
    top: 0; left: 0; right: 0;
    height: 2px;
    background: linear-gradient(90deg, var(--accent), var(--accent2));
    opacity: 0;
    transition: opacity 0.3s;
  }

  .skill-card:hover {
    transform: translateY(-4px);
    border-color: rgba(0,229,255,0.3);
    box-shadow: 0 16px 40px rgba(0,0,0,0.4), var(--glow);
  }

  .skill-card:hover::before { opacity: 1; }

  .skill-images {
    display: grid;
    gap: 2px;
  }

  .skill-images.two-col { grid-template-columns: 1fr 1fr; }
  .skill-images.one-col { grid-template-columns: 1fr; }

  .skill-img {
    width: 100%;
    height: 180px;
    object-fit: cover;
    display: block;
    transition: transform 0.4s ease;
  }

  .skill-images.two-col .skill-img { height: 130px; }

  .skill-card:hover .skill-img { transform: scale(1.04); }

  .skill-body {
    padding: 20px;
  }

  .skill-name {
    font-family: 'Syne', sans-serif;
    font-size: 18px;
    font-weight: 800;
    color: var(--text);
    margin-bottom: 8px;
    display: flex;
    align-items: center;
    gap: 10px;
  }

  .skill-dot {
    width: 8px; height: 8px;
    border-radius: 50%;
    background: var(--accent);
    box-shadow: 0 0 8px var(--accent);
    flex-shrink: 0;
  }

  .skill-desc {
    font-size: 13px;
    color: var(--text-muted);
    line-height: 1.7;
  }

  /* ── ACHIEVEMENT ── */
  .achievement-wrap {
    max-width: 680px;
    margin: 0 auto;
  }

  .achievement-card {
    background: var(--surface);
    border: 1px solid var(--border);
    border-radius: 20px;
    overflow: hidden;
    box-shadow: var(--glow);
    position: relative;
  }

  .achievement-card::before {
    content: '';
    position: absolute;
    inset: 0;
    background: linear-gradient(135deg, rgba(0,229,255,0.04), rgba(124,58,237,0.04));
    pointer-events: none;
  }

  .achievement-img-wrap {
    position: relative;
    overflow: hidden;
  }

  .achievement-img {
    width: 100%;
    max-height: 480px;
    object-fit: contain;
    background: var(--surface2);
    display: block;
    transition: transform 0.5s ease;
  }

  .achievement-card:hover .achievement-img { transform: scale(1.02); }

  .achievement-badge {
    position: absolute;
    top: 16px; left: 16px;
    background: rgba(0,0,0,0.7);
    backdrop-filter: blur(8px);
    border: 1px solid rgba(245,158,11,0.4);
    color: var(--accent3);
    font-family: 'Space Mono', monospace;
    font-size: 10px;
    letter-spacing: 2px;
    padding: 6px 14px;
    border-radius: 20px;
  }

  .achievement-body {
    padding: 28px;
  }

  .achievement-title {
    font-family: 'Syne', sans-serif;
    font-size: 22px;
    font-weight: 800;
    margin-bottom: 10px;
  }

  .achievement-desc {
    font-size: 14px;
    color: var(--text-muted);
    line-height: 1.7;
  }

  /* ── CONTACT FORM ── */
  .contact-section {
    margin-top: 80px;
    padding-top: 60px;
    border-top: 1px solid var(--border);
  }

  .form-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 16px;
    margin-bottom: 16px;
  }

  .form-group { display: flex; flex-direction: column; gap: 6px; }
  .form-group.full { grid-column: 1 / -1; }

  label {
    font-family: 'Space Mono', monospace;
    font-size: 10px;
    color: var(--accent);
    letter-spacing: 1.5px;
    text-transform: uppercase;
  }

  input, textarea {
    background: var(--surface);
    border: 1px solid var(--border);
    border-radius: 8px;
    color: var(--text);
    font-family: 'DM Sans', sans-serif;
    font-size: 14px;
    padding: 12px 16px;
    transition: border-color 0.2s, box-shadow 0.2s;
    outline: none;
    resize: vertical;
  }

  input:focus, textarea:focus {
    border-color: var(--accent);
    box-shadow: 0 0 0 3px rgba(0,229,255,0.08);
  }

  textarea { min-height: 120px; }

  .btn-send {
    display: inline-flex;
    align-items: center;
    gap: 10px;
    background: linear-gradient(135deg, var(--accent), var(--accent2));
    color: #000;
    font-family: 'Syne', sans-serif;
    font-size: 13px;
    font-weight: 700;
    letter-spacing: 1.5px;
    text-transform: uppercase;
    border: none;
    border-radius: 8px;
    padding: 14px 32px;
    cursor: pointer;
    transition: opacity 0.2s, transform 0.2s;
  }

  .btn-send:hover { opacity: 0.9; transform: translateY(-1px); }
  .btn-send:active { transform: translateY(0); }

  .form-note {
    font-size: 12px;
    color: var(--text-muted);
    margin-top: 10px;
  }

  /* ── SUCCESS MSG ── */
  .success-msg {
    display: none;
    align-items: center;
    gap: 10px;
    background: rgba(0,229,255,0.06);
    border: 1px solid rgba(0,229,255,0.25);
    border-radius: 8px;
    padding: 14px 18px;
    font-size: 13px;
    color: var(--accent);
    margin-top: 16px;
  }

  .success-msg.show { display: flex; }

  /* Responsive */
  @media (max-width: 768px) {
    nav { padding: 14px 20px; }
    .nav-logo { display: none; }
    .section { padding: 100px 20px 60px; }
    .home-grid { grid-template-columns: 1fr; }
    .profile-card { position: static; }
    .form-grid { grid-template-columns: 1fr; }
    .info-cards { grid-template-columns: 1fr; }
    .skill-grid { grid-template-columns: 1fr; }
    .nav-tab { padding: 7px 14px; font-size: 11px; }
    .section-title { font-size: 26px; }
  }
</style>
</head>
<body>

<!-- NAVBAR -->
<nav>
  <div class="nav-logo">// fathir.dev</div>
  <div class="nav-tabs">
    <button class="nav-tab active" onclick="showTab('home')">Home</button>
    <button class="nav-tab" onclick="showTab('skill')">Skill</button>
    <button class="nav-tab" onclick="showTab('achievement')">Achievement</button>
  </div>
</nav>

<!-- ══════════════ HOME ══════════════ -->
<section id="home" class="section active">
  <div class="container">
    <div class="home-grid">

      <!-- Sidebar -->
      <div class="profile-card">
        <div class="profile-photo-wrap">
          <img
            src="https://i.ibb.co.com/cKgHq87r/IMG-20260524-WA0031.jpg"
            alt="Foto Profil Fathir"
            class="profile-photo"
            onerror="this.style.background='var(--surface2)';this.alt='Foto';this.src='data:image/svg+xml,<svg xmlns=\'http://www.w3.org/2000/svg\' width=\'140\' height=\'140\'><rect fill=\'%231a1a24\' width=\'140\' height=\'140\'/><text x=\'50%25\' y=\'55%25\' fill=\'%2300e5ff\' font-size=\'40\' text-anchor=\'middle\' dominant-baseline=\'middle\' font-family=\'sans-serif\'>F</text></svg>'">
        </div>
        <div class="profile-name">Mochammad Fathir Reza Fahresi</div>
        <div class="profile-badge">X TKJ 1</div>
        <div class="profile-school">SMK PLUS PELITA NUSANTARA<br>Teknik Komputer & Jaringan</div>
        <div class="contact-links">
          <a href="mailto:fahresifathirreza@gmail.com" class="contact-link">
            <span class="contact-icon">✉</span>
            fahresifathirreza@gmail.com
          </a>
          <a href="tel:0895374244050" class="contact-link">
            <span class="contact-icon">☏</span>
            0895374244050
          </a>
        </div>
      </div>

      <!-- About -->
      <div class="about-content">
        <div class="section-label">// Tentang Saya</div>
        <h1 class="section-title">Halo, Saya <span>Fathir</span> 👋</h1>
        <div class="divider"></div>

        <p class="about-text">
          Nama saya <strong>Mochammad Fathir Reza Fahresi</strong>, seorang siswa aktif kelas <strong>X TKJ 1</strong> di <strong>SMK PLUS PELITA NUSANTARA</strong>, mengambil jurusan <strong>Teknik Komputer dan Jaringan (TKJ)</strong>. Sejak duduk di bangku sekolah menengah pertama, saya sudah sangat tertarik dengan dunia teknologi, terutama bagaimana komputer dan jaringan bekerja di balik layar.
        </p>

        <p class="about-text">
          Keputusan saya masuk jurusan TKJ bukan sekadar mengikuti tren, melainkan lahir dari rasa ingin tahu yang mendalam. Saya selalu terpesona saat melihat bagaimana sebuah jaringan internet dibangun — dari kabel fiber optik yang menjalar di bawah tanah, router yang mengatur lalu lintas data, hingga server yang melayani jutaan permintaan setiap detiknya. Saya ingin memahami dan menguasai semua itu secara menyeluruh.
        </p>

        <p class="about-text">
          Di era digital seperti sekarang, keahlian di bidang jaringan dan infrastruktur IT menjadi kebutuhan yang sangat vital. Hampir semua sektor — pendidikan, bisnis, kesehatan, pemerintahan — bergantung pada konektivitas dan sistem jaringan yang handal. Dengan masuk TKJ, saya tidak hanya belajar teori, tetapi langsung mempraktikkan keahlian nyata seperti konfigurasi Mikrotik, splicing fiber optik, dan manajemen server. Ini adalah bekal yang saya yakini akan sangat berharga di dunia kerja.
        </p>

        <p class="about-text">
          Selain itu, saya termotivasi oleh cita-cita untuk menjadi seorang <strong>Network Engineer</strong> atau <strong>IT Infrastructure Specialist</strong> yang profesional. Saya percaya bahwa fondasi yang kuat dimulai dari pendidikan vokasi yang tepat sasaran. SMK PLUS PELITA NUSANTARA memberikan lingkungan belajar yang mendukung, dengan fasilitas praktik yang memadai dan guru-guru berpengalaman di bidangnya.
        </p>

        <p class="about-text">
          Bagi saya, TKJ bukan sekadar jurusan — ini adalah <strong>pintu gerbang menuju karier impian</strong> di bidang teknologi informasi. Setiap pelajaran yang saya terima, setiap praktik yang saya lakukan, adalah investasi untuk masa depan yang lebih cerah dan bermakna.
        </p>

        <div class="info-cards">
          <div class="info-card">
            <div class="info-card-label">Sekolah</div>
            <div class="info-card-value">SMK PLUS PELITA NUSANTARA</div>
          </div>
          <div class="info-card">
            <div class="info-card-label">Jurusan</div>
            <div class="info-card-value">Teknik Komputer & Jaringan</div>
          </div>
          <div class="info-card">
            <div class="info-card-label">Kelas</div>
            <div class="info-card-value">X TKJ 1</div>
          </div>
          <div class="info-card">
            <div class="info-card-label">Status</div>
            <div class="info-card-value">Siswa Aktif 2025/2026</div>
          </div>
        </div>
      </div>
    </div>

    <!-- Contact Form (bottom of Home) -->
    <div class="contact-section">
      <div class="section-label">// Hubungi Saya</div>
      <h2 class="section-title" style="font-size:28px; margin-bottom:8px;">Kirim <span>Pesan Cepat</span></h2>
      <div class="divider"></div>
      <p class="about-text">Punya pertanyaan atau ingin berkolaborasi? Isi form di bawah dan pesan Anda akan langsung terkirim ke email saya.</p>

      <div class="form-grid">
        <div class="form-group">
          <label for="fn">Nama Lengkap</label>
          <input type="text" id="fn" placeholder="Masukkan nama lengkap Anda">
        </div>
        <div class="form-group">
          <label for="fp">Nomor Telepon</label>
          <input type="tel" id="fp" placeholder="Contoh: 0812xxxxxxxx">
        </div>
        <div class="form-group">
          <label for="fe">Email</label>
          <input type="email" id="fe" placeholder="email@contoh.com">
        </div>
        <div class="form-group">
          <label>&nbsp;</label>
          <!-- spacer -->
        </div>
        <div class="form-group full">
          <label for="fm">Pesan</label>
          <textarea id="fm" placeholder="Tuliskan pesan Anda di sini..."></textarea>
        </div>
      </div>

      <button class="btn-send" onclick="sendEmail()">
        <span>✈</span> Kirim Pesan
      </button>
      <p class="form-note">Pesan akan dikirim melalui aplikasi email Anda ke: fahresifathirreza@gmail.com</p>

      <div class="success-msg" id="successMsg">
        ✓ Pesan berhasil disiapkan! Selesaikan pengiriman di aplikasi email Anda.
      </div>
    </div>
  </div>
</section>

<!-- ══════════════ SKILL ══════════════ -->
<section id="skill" class="section">
  <div class="container">
    <div class="page-header">
      <div class="section-label">// Keahlian Saya</div>
      <h1 class="section-title">Tech <span>Skills</span></h1>
      <div class="divider"></div>
      <p class="about-text">Berikut adalah keahlian teknis yang telah saya pelajari dan praktikkan selama mengikuti pendidikan di jurusan TKJ.</p>
    </div>

    <div class="skill-grid">

      <!-- Mikrotik -->
      <div class="skill-card">
        <div class="skill-images two-col">
          <img class="skill-img" src="https://i.ibb.co.com/nq15x5Dm/IMG-20251218-WA0014-2.jpg" alt="Mikrotik 1"
            onerror="this.src='data:image/svg+xml,<svg xmlns=\'http://www.w3.org/2000/svg\' width=\'200\' height=\'130\'><rect fill=\'%231a1a24\' width=\'200\' height=\'130\'/><text x=\'50%25\' y=\'50%25\' fill=\'%2300e5ff\' text-anchor=\'middle\' dominant-baseline=\'middle\' font-size=\'12\' font-family=\'sans-serif\'>Mikrotik</text></svg>'">
          <img class="skill-img" src="https://i.ibb.co.com/5fvKQqM/Screenshot-2026-03-31-08-46-00-911-com-miui-gallery.jpg" alt="Mikrotik 2"
            onerror="this.src='data:image/svg+xml,<svg xmlns=\'http://www.w3.org/2000/svg\' width=\'200\' height=\'130\'><rect f